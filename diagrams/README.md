# Text-editable architecture sources

These Mermaid diagrams describe the same boundaries as the visual atlas. Render with a Mermaid-compatible Markdown viewer or CLI. The styled SVG/PNG plates are generated separately by `scripts/render_diagrams.py`; changing a Mermaid file does not automatically update a plate. Keep both representations aligned.
