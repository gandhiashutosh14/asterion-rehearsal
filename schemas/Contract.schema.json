{
  "$defs": {
    "Obligation": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "pattern": "^R-[0-9]{2}$",
          "title": "Id",
          "type": "string"
        },
        "title": {
          "maxLength": 180,
          "minLength": 3,
          "title": "Title",
          "type": "string"
        },
        "owner": {
          "maxLength": 80,
          "minLength": 1,
          "title": "Owner",
          "type": "string"
        },
        "critical": {
          "default": true,
          "title": "Critical",
          "type": "boolean"
        },
        "required_slices": {
          "items": {
            "type": "string"
          },
          "maxItems": 20,
          "minItems": 1,
          "title": "Required Slices",
          "type": "array"
        }
      },
      "required": [
        "id",
        "title",
        "owner",
        "required_slices"
      ],
      "title": "Obligation",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "id": {
      "pattern": "^[a-z0-9-]{1,60}$",
      "title": "Id",
      "type": "string"
    },
    "version": {
      "maxLength": 40,
      "minLength": 1,
      "title": "Version",
      "type": "string"
    },
    "customer": {
      "maxLength": 100,
      "minLength": 1,
      "title": "Customer",
      "type": "string"
    },
    "objective": {
      "maxLength": 1000,
      "minLength": 10,
      "title": "Objective",
      "type": "string"
    },
    "data_classification": {
      "const": "synthetic",
      "default": "synthetic",
      "title": "Data Classification",
      "type": "string"
    },
    "obligations": {
      "items": {
        "$ref": "#/$defs/Obligation"
      },
      "maxItems": 40,
      "minItems": 1,
      "title": "Obligations",
      "type": "array"
    }
  },
  "required": [
    "id",
    "version",
    "customer",
    "objective",
    "obligations"
  ],
  "title": "Contract",
  "type": "object"
}
